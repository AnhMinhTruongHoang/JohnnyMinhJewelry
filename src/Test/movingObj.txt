"use client";

import { Environment, OrbitControls } from "@react-three/drei";
import { useRef } from "react";
import { Group } from "three";
import gsap from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";
import { useGSAP } from "@gsap/react";
import { useMediaQuery } from "@/hooks/useMediaQuery";
import FloatingRing from "@/Components/floating.ring";

gsap.registerPlugin(ScrollTrigger, useGSAP);

type Props = {};

export default function Scene({}: Props) {
  const canRef = useRef<Group>(null);
  const isDesktop = useMediaQuery("(min-width: 768px)", true);

  // 🎨 Màu background cho từng section (có thể thêm nhiều màu)
  const bgColors = ["#E9CFF6"];

  useGSAP(
    () => {
      if (!canRef.current) return;

      const sections = gsap.utils.toArray(".alternating-section");

      const scrollTl = gsap.timeline({
        scrollTrigger: {
          trigger: ".alternating-text-view",
          endTrigger: ".alternating-text-container",
          pin: true,
          start: "top top",
          end: "bottom bottom",
          scrub: true,
        },
      });

      sections.forEach((_, index) => {
        if (!canRef.current) return;
        if (index === 0) return;

        const isOdd = index % 2 !== 0;

        // 👉 Tùy chỉnh chuyển động theo section:
        // xPosition: di chuyển vật thể theo trục X
        // yRotation: xoay vật thể theo trục Y
        const xPosition = isDesktop ? (isOdd ? "-1" : "1") : 0;
        const yRotation = isDesktop ? (isOdd ? ".4" : "-.4") : 0;

        scrollTl
          .to(canRef.current.position, {
            x: xPosition, // 👈 đổi số này để vật thể trượt nhiều hay ít
            ease: "circ.inOut",
            delay: 0.5,
          })
          .to(
            canRef.current.rotation,
            {
              y: yRotation, // 👈 đổi số này để vật thể xoay mạnh hay nhẹ
              ease: "back.inOut",
            },
            "<",
          )
          .to(".alternating-text-container", {
            backgroundColor: gsap.utils.wrap(bgColors, index),
          });
      });
    },
    { dependencies: [isDesktop] },
  );

  return (
    <group
      ref={canRef}
      // 👉 Vị trí ban đầu của vật thể:
      // tăng/giảm position-x, y, z để đổi chỗ ring
      position-x={isDesktop ? 1 : 0}
      position-y={0} // 👈 lên/xuống
      position-z={0} // 👈 ra trước / lùi sau camera
      // 👉 Góc xoay ban đầu (radian):
      rotation-y={isDesktop ? 0 : -0.3} // 👈 xoay qua trái/phải
      rotation-x={0} // 👈 ngửa lên/xuống
      rotation-z={0} // 👈 xoay nghiêng
      // 👉 Scale toàn bộ group:
      scale={1.2} // 👈 tăng lên 2.0 cho to hơn
    >
      <FloatingRing />
      <Environment files={"/HDR/lobby.hdr"} environmentIntensity={1.5} />
    </group>
  );
}
